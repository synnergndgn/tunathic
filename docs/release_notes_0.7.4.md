# Tunathic 0.7.4+14

- Unified every screen on the studio shell, so the chord, scale, fretboard,
  circle, theory and repertoire pages carry the same workbench backdrop,
  app-bar face and content width as the tuner and metronome.
- Stopped dashboard dock labels from breaking mid-word; a long tool name now
  shrinks to fit instead of splitting.
- Replaced the truncated metronome volume label with an engraved numeric
  readout. The full percentage still reaches screen readers.
- Added the Google Play store asset system: app icon and feature graphic
  masters, the screenshot capture and composition pipeline, and the visual
  direction it all follows.
